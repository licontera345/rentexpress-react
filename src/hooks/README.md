# Hooks

Los hooks en este proyecto deben usar hooks de React (por ejemplo, `useState`, `useEffect`).
Si un helper no necesita hooks, mantén una función normal en `utils/` en vez de un hook.

`useApi` se eliminó porque no aportaba lógica adicional más allá de envolver `useEffect`.
